import React, { Component } from 'react'
import { Text, View } from 'react-native'

export default class NetworksListScreen extends Component {
    render() {
        return (
            <View>
                <Text> NetworksList </Text>
            </View>
        )
    }
}
